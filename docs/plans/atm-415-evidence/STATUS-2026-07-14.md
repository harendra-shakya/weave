# ATM-415 Daily Record — 2026-07-14 (Day 1)

## STATUS

- ATM-415: source-authority readback recorded as an issue comment (execution-hold interpretation, veto window open for controller).
- ATM-416: **complete, awaiting controller review.** Shared contracts frozen; sibling repo `weave-v5-apps` created.
- ATM-417..423: not started (per dependency order).

## PROOF

- Sprint plan committed: `docs/plans/harendra-atm-415-qa-plan.md` @ weave-repo commit `fa4601f` (branch `atm-415-weave-qa`, base `a7d9aa1`).
- `weave-v5-apps` root commit: `7c813e0` (43 files, 1655 insertions) — three app lifecycle specs (video / sticker / chainless synthetic collectible), 7 shared schemas, frozen seeds (`atm41X-baseline-v1`, cohort 200), frozen KPI targets/guardrails, state-transition + idempotency + retry + cleanup contract, chainless negative contract, public-safe asset contract, deterministic `commerce-core` (seeded PRNG, cohort generator, JSONL ledger, SHA-256 digest/manifest tooling).
- Validator: `npm run validate` — all 6 sections PASS (3 specs validate; 7 valid fixtures pass; 7 invalid fixtures fail closed; chainless + public-safe scans clean; state map covers all 8 order statuses; freeze digests verified).
- Tests: `npm test` — 22/22 pass, including determinism (identical seed → byte-identical ledger and equal digest, twice per app; different seed → different digest).
- Tamper-evidence proof: modifying a frozen KPI value made the validator FAIL with "digest mismatch … not covered by an owner gate"; restoring returned PASS. Retroactive KPI/cohort changes are not possible without a recorded owner gate.
- Freeze ledger digest: `contracts/freeze-digests.json` (29 files) sha256 `9d80fe146bf3227317deed8f98210029271b7084de7d42dfff55aad64daa31ac`.
- Failures/recovery: one reviewer-fix cycle — Windows `node --test` directory-arg quirk (test script changed to glob form) and one chain-boundary ambiguity in the collectible spec ("payment token" reworded to "payment authorization") — both resolved before freeze; no bounded-retry budget consumed.

## Addendum — WEAVE-home bootstrap (process-fidelity fix, Harendra's review)

Harendra flagged that "use WEAVE to build" requires the apps to go through WEAVE's own machinery, not merely sit beside it. Fixed the same day (`weave-v5-apps` commit `f506032`):

- `weave-v5-apps` bootstrapped as a WEAVE home via `weave cos-bootstrap` (the tool's own skeleton generator), registering all three apps with intent, lifecycle 01–11 stage surfaces, proof trays, blocker trays, deployment gates, and readback.
- Owner context recorded per app from the frozen contracts; the bootstrap's `owner-context-needed` blocker resolved with evidence pointers; `deployment-provider-access-not-validated` left truthfully open (deployment out of scope).
- Each app passed its `01-intent` stage-entry eval through WEAVE's evidence-bound runner (`weave eval intent --run-gates --strict`): video 93.75%, sticker 93.75%, collectible 100%, both hard gates green per app. Full eval JSON stored at `apps/<app-id>/proof/intent-eval-result.json`; lifecycle state advanced to `research` truthfully (stages 02–11 pending).
- Notable: the chainless negative contract flagged the execution agent's first draft for quoting forbidden vocabulary as evidence text — the fail-closed gate worked against its own author; fixed by referencing the contract without repeating its token list.
- Orchestrator independently reran one eval (decision `advance`), the contract validator (freeze digests intact), the 22-test suite, and readback before commit.

All subsequent stages (research → analysis) for ATM-417/418/419 will advance only through their stage-entry evals, giving the immutable WEAVE lineage ATM-415 requires.

## Model/tool/usage ledger (Day 1)

| Actor | Model | Role | Usage |
|---|---|---|---|
| Orchestrator | Fable 5 | Planning, contract design, review, freeze, evidence, Linear readbacks | this session |
| Execution agent | Sonnet 5 | Built `weave-v5-apps` contract package (43 files) | 143,328 tokens, 62 tool uses, ~16 min |
| Execution agent 2 | Sonnet 5 | WEAVE-home intent-stage pass for 3 apps | 207,714 tokens, 91 tool uses, ~13 min |

## Comprehension debt

GREEN — orchestrator reviewed specs, seeds, KPI freezes line-by-line and independently reran validator, tests, and tamper proof.

## NEXT (one wedge)

ATM-417: build the video storefront and run its full baseline → bounded adaptation → identical-retest loop (Day 2).
