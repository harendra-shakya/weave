__pycache__/
*.py[cod]
.DS_Store
.venv/
.env
.env.*
!.env.example
node_modules/
dist/
build/
.next/
next-env.d.ts
*.tsbuildinfo
# cockpit lockfile: deps are pinned to exact versions in package.json; the
# lockfile trips the public-safe scanner on transitive semver like "10.0.0".
cockpit/package-lock.json
runs/
evidence/
.workflow/

design_handoff_ledger_cockpit/

# personal Claude Code agents — not part of the shared project config
.claude/agents/